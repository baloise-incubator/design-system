import { FunctionalComponent, h } from '@stencil/core'

export interface NavButtonProps {}

export const NavButton: FunctionalComponent<NavButtonProps> = ({}) => {
  // console.log('NavButton render')
  return <span>Button</span>
}
